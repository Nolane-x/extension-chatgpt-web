# v0.3.0 version bump

Version authority for the v0.3.0 release is synchronized across:

- `manifest.json`
- `package.json`
- `native-host/nolane_bridge.mjs`

The release packager derives artifact names from `package.json`; the verifier rejects any manifest/package/native-host mismatch.
